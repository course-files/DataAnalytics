# Data Analytics

The default installation directory is set in this file:
[.Rprofile](/.Rprofile)

Execute the following in an R terminal to verify where R is going to install the user libraries:

```R
Sys.getenv("R_LIBS_USER")
.libPaths()
```
