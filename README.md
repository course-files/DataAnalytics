# DataAnalytics

The default installation directory is set here:
[.Rprofile](/.Rprofile)

Execute the following in an R terminal to verify where R is going to install user libraries:

```R
Sys.getenv("R_LIBS_USER")
.libPaths()
```
